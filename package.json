{
  "name": "life-decision-slot-machine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm install && vite --host 0.0.0.0",
    "start": "npm run dev",
    "build": "npm install && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "vite": "5.4.21"
  }
}
