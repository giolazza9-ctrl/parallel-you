# Life Decision Slot Machine

A premium-feeling casino-style decision generator for overthinkers.

## Run

```bash
npm install
npm run dev
```

The app is a vanilla Vite site using `index.html`, `styles.css`, and `script.js`.
